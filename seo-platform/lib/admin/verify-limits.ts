// 1단계(업체 확인) 한 번에 처리 상한 — 서버 액션과 화면이 같은 값을 본다.
//
// 서버 쪽 구현(lib/admin/verification-queue.ts)은 "server-only"라 클라이언트 컴포넌트가
// import할 수 없어 상수가 양쪽에 복제돼 있었다. 값이 어긋나면 화면은 허용하는데 서버가
// 잘라내는 형태로 조용히 갈라진다 — 2026-08-06 DB CHECK(5) vs 앱 상한(20) 사고와 같은 유형이라
// 상수만 별도 모듈로 떼어 한 곳에서 관리한다.
//
// 10 → 20 (2026-08-06): 2단계 AI 생성 상한(BATCH_MAX_ITEMS=20)과 맞춰 한 번에 확인·등록할 수 있게 한다.
export const VERIFY_MAX_ITEMS = 20
